<!-- 退会していない時の処理(is_deleted:false) -->
<% if @user.is_deleted == false %>
  <!-- ユーザーのプロフィール -->
  <%= render 'info', user: @user %>
  <div class="container px-sm-0">
    <h2 class="my-4 ml-5 pl-5">
      <strong><%= @bookmark_posts.count %>件のブックマーク</strong>
    </h2>
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="list-group" style= "width: 100%">
          <!-- ブックマークした投稿一覧-->
          <%= render 'public/bookmarks/bookmark_list', bookmark_posts: @bookmark_posts %>
        </div>
      </div>
    </div>
  </div>
<!--退会している場合の処理(is_deleted:true)-->
<% else %>
  <h3 class="text-center"><strong>退会済みユーザーです</strong></h3>
<% end %>