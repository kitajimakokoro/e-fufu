<div class="container px-3 px-sm-0">
  <h1 class="my-5">
    <strong>"<%= @keyword %>"の検索結果</strong>
  </h1>
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="row mb-3">
          <!--  検索窓  -->
          <%= render 'search' %>
          <!-- 新規登録ボタン-->
          <div class="form-group col-3 ml-auto text-right">
            <%= link_to "投稿する", new_post_path, class: "btn btn-block", style:"color:#FFFFFF; background-color:#696969;" %>
          </div>
        </div>
        <div class="list-group" style= "width: 100%">
          <!-- キーワードが入力されている場合以下の処理を行う-->
          <% if @keyword.first %>
            <!-- 検索結果が1件以上ある場合(any?でtrue)-->
            <% if @posts.any? %>
              <!-- 該当する投稿一覧表示 -->
              <%= render 'public/posts/list', posts: @posts %>
            <!-- 検索結果が0件の時(any?でfalse)-->
            <% else %>
              <h3>"<%= @keyword %>"の検索結果はありません。</h3>
              <p>検索条件を変更して再度お試しください。</p>
            <% end %>
          <% end %>
        </div>
        <div class="d-flex justify-content-center mt-2">
          <%= paginate @posts %>
        </div>
      </div>
    </div>
</div>
